fun main(args : Array<String>){

    var str : String = "4" // Declare and Assigned String variable.
    var num = str.toInt() // Cast it to an Int. We use toInt() to cast a String to Int.

    num++ // Add 1 to the Int value.
    println(num) // Print the value.
}