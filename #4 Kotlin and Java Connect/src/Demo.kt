fun main(args : Array<String>){

    var obj = Test() //Create an object of Java class (Test)

    obj.name = "Osusara" //Automatically call the setter method and assign the value.
    println(obj.name) //Automatically call the getter method and return the value.
}